[project]
name = "istrip"
version = "0.3.1"
description = "Home Assistant integration for controlling iStrip+ BLE LED lights"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.1",
]
