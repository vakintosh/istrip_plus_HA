[project]
name = "istrip"
version = "0.5.0"
description = "Home Assistant integration for controlling iStrip+ BLE LED lights"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    # Pinned: ruff's default rule set changes between minor releases, so an
    # unpinned range makes local runs disagree with CI.
    "ruff==0.15.1",
]
